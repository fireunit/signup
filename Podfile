platform :ios, '9.0'

target 'signup' do
  use_frameworks!
  # Pods for signup
  pod 'Firebase'
  pod 'Firebase/Auth'
  
  pod 'Material', '~> 1.0'
  
  target 'signupTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'signupUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
